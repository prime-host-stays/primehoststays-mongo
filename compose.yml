networks:
  phs-rest-dev:
    name: phs-rest-dev
    driver: bridge
    # Created by the deployer on the shared target; compose will not create it.
    external: true

services:
  mongo:
    image: mongo:8.3.11-noble
    restart: unless-stopped
    networks:
      - phs-rest-dev
    ports:
      # Loopback bind only: satisfies the deployer inspect contract without
      # external exposure. APIs reach the service as `mongo:27017` on the
      # phs-rest-dev bridge, not via this port. Long-form so the deployer's
      # compose inspect can resolve an explicit `target`.
      - target: 27017
        host_ip: 127.0.0.1
        published: "${MONGO_PORT:-27017}"
        protocol: tcp
        mode: host
    volumes:
      - phs-mongo-data:/data/db
      - ./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d:ro
    healthcheck:
      # TCP/CLI probe: mongod 8 has no HTTP endpoint.
      test: ["CMD-SHELL", "mongosh --quiet --eval 'db.adminCommand(\"ping\").ok' || exit 1"]
      interval: 15s
      timeout: 10s
      start_period: 30s
      retries: 5
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME:?MONGO_INITDB_ROOT_USERNAME is required}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD:?MONGO_INITDB_ROOT_PASSWORD is required}
      MONGO_INITDB_DATABASE: phs
      PHS_COM_API_USERNAME: phs_com
      PHS_COM_API_PASSWORD: ${PHS_COM_API_PASSWORD:?PHS_COM_API_PASSWORD is required}
      PHS_ADMIN_API_USERNAME: phs_admin
      PHS_ADMIN_API_PASSWORD: ${PHS_ADMIN_API_PASSWORD:?PHS_ADMIN_API_PASSWORD is required}

volumes:
  phs-mongo-data:
